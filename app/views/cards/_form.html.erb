<%= form_for(card) do |f| %>
  <% if card.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(card.errors.count, "error") %> prohibited this card from being saved:</h2>

      <ul>
      <% card.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.label "Enter a question:" %>
  <%= f.text_field :question %>

  <%= f.label :answer %>
  <%= f.text_field :answer %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
